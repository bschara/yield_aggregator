# Aptos node
APTOS_RPC=http://localhost:8080/v1

# Deployed contract addresses (fill after aptos move deploy)
APTOS_MODULE_ADDR=0x
VAULT_ADDR=0x
REGISTRY_ADDR=0x
ENGINE_ADDR=0x

# Operator key (hex, no 0x prefix)
OPERATOR_PRIVATE_KEY=

# Ethereum node
ETH_RPC=http://localhost:8545
VAULT_OFT_ADDR=0x

# Python policy server
POLICY_SERVER_URL=http://localhost:8001

# Orchestrator tuning
LOOP_INTERVAL_MS=30000
BRIDGE_TIMEOUT_SEC=600
MIN_DEPLOY_AMOUNT=100000000
HARVEST_MIN_INTERVAL_SEC=3600
